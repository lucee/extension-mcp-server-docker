services:
  lucee:
    build: .
    image: lucee-mcp:latest
    environment:
      - LUCEE_ADMIN_PASSWORD=qwerty
    ports:
      - "8856:8888"  # tomcat
      - "8056:80"    # nginx

# docker compose up -d --build
